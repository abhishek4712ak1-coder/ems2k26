{
  "name": "zest-2k26",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --include=dev",
    "build": "npm run build --workspace client",
    "build:all": "npm run install:all && npm run build --workspace client && npm run build --workspace server",
    "start": "npm start --workspace server",
    "dev": "concurrently --names server,client \"npm run dev --workspace server\" \"npm run dev --workspace client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}